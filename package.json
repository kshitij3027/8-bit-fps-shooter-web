{
  "name": "8bit-fps-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild game.js --bundle --minify --sourcemap --outfile=game.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}